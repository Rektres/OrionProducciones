# DATABASE
DATABASE_URL=postgresql://postgres:SECURE_PASSWORD@postgres:5432/orion
POSTGRES_PASSWORD=SECURE_PASSWORD

# DJANGO
DJANGO_SECRET_KEY=LONG_RANDOM_SECRET_KEY_CHANGE_IN_PROD
DEBUG=False
# El postgres de docker-compose no tiene SSL habilitado (red interna Docker)
DB_SSL_REQUIRE=False

# IP donde se publica el puerto 5432 de Postgres (para conectar con pgAdmin/DBeaver).
# Default 127.0.0.1 (solo accesible con un túnel SSH). Si usas Tailscale, pon la IP
# de tailscale0 de este servidor (ver `tailscale ip -4`) para conectar directo sin túnel.
PG_BIND_IP=127.0.0.1

# IP pública del servidor (sin dominio todavía)
SERVER_IP=203.0.113.10

# HOSTS / CORS (acceso vía IP, sin dominio)
ALLOWED_ORIGINS=http://203.0.113.10

# Nota: VITE_API_URL ya está fijado en frontend-vue/.env.production como
# ruta relativa (/api), por lo que funciona con cualquier IP o dominio
# sin necesidad de rebuild. No hace falta definirlo aquí.

# EMAIL (notificación de nuevas cotizaciones)
# Usa el SMTP de tu propio dominio/hosting (Zoho, cPanel, etc.) — cualquier
# proveedor SMTP funciona igual, no hay que atarse a un SDK específico.
# Puerto 587 -> EMAIL_USE_TLS=True, EMAIL_USE_SSL=False
# Puerto 465 -> EMAIL_USE_TLS=False, EMAIL_USE_SSL=True (son mutuamente excluyentes)
EMAIL_HOST=smtp.tudominio.cl
EMAIL_PORT=587
EMAIL_HOST_USER=contacto@tudominio.cl
EMAIL_HOST_PASSWORD=CONTRASENA_DEL_CORREO
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
# Debe ser un buzón real de la cuenta autenticada, o el SMTP rechaza el envío.
DEFAULT_FROM_EMAIL=contacto@tudominio.cl
# A quién se le avisa cuando llega una cotización nueva.
ADMIN_NOTIFICATION_EMAIL=admin@tudominio.cl
