# Usado por Vite en `npm run build` dentro de Docker.
# Ruta relativa: funciona sin importar el host (localhost, IP o dominio),
# ya que Nginx sirve el frontend y el proxy /api en el mismo origen.
VITE_API_URL=/api
VITE_WHATSAPP_NUMBER=56998249498
VITE_BASE=/
